// Crear una función filter() que acepte un array y un callback y que:
// - Por cada elemento del array ejecute el callback pasándole dicho elemento como argumento
// - Si dicho elemento callback devuelve 'true', agreagr el elemento a un nuevo array
// - Devolver array final con los elementos que pasaron el filtro