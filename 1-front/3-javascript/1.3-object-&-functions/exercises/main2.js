// 2- Crear función que devuelva el nombre completo de una persona (concat strings)

function getFullName(firstName, lastName) {
    let fullName = firstName + " " + lastName;
    return fullName;
}

console.log(getFullName("Luis", "Rodríguez"));